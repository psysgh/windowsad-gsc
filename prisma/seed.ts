// Não há dataset a popular — o cenário é derivado da seed em tempo real
// (src/lib/seedDataset.ts). Este arquivo existe apenas para satisfazer
// integrações de tooling que esperam um seed script.

console.log("[ground-station-compromise] sem seed estática — datasets são derivados em runtime.");
